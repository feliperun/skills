interface:
  display_name: "Plan Runner"
  short_description: "Run multi-model plans with quality gates"
  default_prompt: "Use $plan-runner to execute this plan as a routed, observable graph."
  keywords:
    - "run plan"
    - "multi-model DAG"
    - "quality gate"
    - "campaign"
    - "task packet"
    - "worker result"
    - "blocked_context"
    - "resume"
    - "cancel run"
    - "doctor"
    - "JSON status"
    - "exec-jsonl"
    - "glm"
