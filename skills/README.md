# Skills

All skills live in [`mine/`](mine/), one folder per skill with a `SKILL.md` at
its root. Scripts, references, and templates stay inside the skill folder so a
skill remains a single copyable unit.

Installation: `npx github:feliperun/skills` (see the root `README.md`).
