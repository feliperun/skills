# Curated

Community skills adapted to a personal workflow.

For every skill in this bucket, document in its folder:

- the upstream source (repo and commit/version),
- what changed and why,
- how to pull upstream updates (re-apply the diff, then upgrade).

Never edit a curated skill silently: the change log is what makes it curated
instead of `mine/`.
