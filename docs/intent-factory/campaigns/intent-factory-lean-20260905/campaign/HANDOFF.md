# campaign intent-factory-lean-20260905 handoff

Updated: 2026-09-08T20:02:09.356Z

## Goal

Ship intent-factory v0.3 lean per docs/intent-factory/TECH-SPEC-2026-09-05-lean.md: whole-repo worktrees, verification as the gate with advisory review, USD budgets, retry in place, single controller process, direct notifications, redesigned dashboard and status surfaces, docs and code diet; measured against the 0.2.0 campaign baseline

## Latest next action

- Continue from the other machine: clone feliperun/skills at campaign/lean-v03 (88064c4), npm ci, restore .runs/campaigns and .runs/control from docs/intent-factory/campaigns/intent-factory-lean-20260905/, replace the two absolute paths in the copied control files, run the full suite, then launch phase 4 (runner-split, docs-v03, metrics-v03; Sonnet workers, deepseek-v4-pro advisory judge) with launch-phase.sh, land the run ref, close phase 4, and run the campaign close (0.3.0, retrospective, campaign close, push). Full steps in control/ORCHESTRATOR-HANDOFF-20260908.md, section 'continue from another machine'. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T20:02:09.356Z

## Session lineage

- claude-code 43c14aac-b457-4102-b11a-9076f120f008 · transcript: /Users/frb/.claude/projects/-Users-frb-dev-frb-skills-finish-camp/43c14aac-b457-4102-b11a-9076f120f008.jsonl · format: jsonl · cursor: -
- claude-code f89ac1cf-fc09-4bba-a023-843a68440f44 · transcript: /Users/frb/.claude/projects/-Users-frb-dev-frb-skills/f89ac1cf-fc09-4bba-a023-843a68440f44.jsonl · format: jsonl · cursor: -
- codex 01a07dea-daad-7292-af72-3ef8aedc53b0 · transcript: unavailable · format: - · cursor: -
- claude-code fcc2f493-0e59-43d7-a4e6-9a7b8958080a · transcript: /Users/frb/.claude/projects/-Users-frb-dev-frb-skills/fcc2f493-0e59-43d7-a4e6-9a7b8958080a.jsonl · format: jsonl · cursor: -

## Active decisions

- [deepseek-review-2a-round1] Phase-2a contract review round 1 by deepseek-v4-pro (read-only codex sandbox, reasoning high, about 12 minutes, 19.4M input of which 19.0M cached, 109k output): HOLD with 10 findings, 2 blocking. All accepted and folded into phase-2a.contract.json. F1/F2 (blocking): capsule.mjs deletion collided with the handoff CLI (handoffRun, phaseInvocationPlan, capsuleHandoffPrompt, loadLatestCapsule, test/handoff.test.mjs) and with a DoD that called provider continuation 'untouched'; decision: delete the handoff command and its tests with the capsule (rule 8 re-tiering plus resume cover re-routing) and compose fresh continuation prompts from node state (previous error, findings, changed files) as rule 4 says. F3: campaign-autonomy.mjs carries about 32 references to deleted fields (usagePolicy, budgetProfile, progressPolicy, plan authority ceilings, its own ledger reader); enumerated for minimal adaptation, module stays until 2b. F4: TREE_CONDITION_PLACEHOLDER is filled after p1f closes. F5: weighted and budget indicators (weightedPerClosedCheckpoint, budgetDecisionAge, budgetHeadroomAtDispatch, budgetExtensionRate, budgetAttentionLatencyP95) and heartbeat weightedUsed/weightedCap named for deletion. F6-F8: wrong file hint, token-kind vocabulary aligned to 'uncached input, cache read, output', ledger location corrected to .runs/campaigns/<id>/usage-ledger.json. F9: test/*.mjs glob measured at 448 s summed, largest file 113 s, eight cores, so one 600 s entry holds. F10: PROTOCOL_SCHEMA_VERSION in drivers/index.mjs named as the constant to move to 3; INTENT_FACTORY_VERSION stays 0.1.0 until release. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T04:40:26.761Z
- [rotation-loses-worktree-edits-20260908] Phase-1 runner defect found in the field on p1f attempt 1: automatic session rotation (ROTATION_MAX_TURNS_CLAUDE_FAMILY = 600 observed turns) fired at 05:06Z after 58 minutes of a healthy Sonnet worker iterating on tests, and the rotation handoff re-dispatched the node as attempt 2 in a NEW attempt worktree cut from the run ref (85d53c9), while attempt 1's uncommitted work (5 files, 229 insertions, 51 deletions: 15 root causes fixed, replay 33/33, handoff 8/8, intent-settlement 11/11) stayed behind in .runs/worktrees/<run>/suite-fix-phase1.1. The handoff markdown lists 'files touched so far (uncommitted)' as if they were present, so the fresh session started redoing them (58 insertions within five minutes). The same loss hit p1e attempt 3 on 2026-09-08T01:12Z ('the checkpoint itself contains none of the prior uncommitted implementation; the earlier worktrees do contain a candidate patch'). Under worktrees, every rotation or fresh continuation of a Claude worker past 600 turns throws away the attempt's edits. Actions: run canceled at 05:13Z; attempt 1's diff applied to the campaign branch as a checkpoint commit (same pattern as fc7e9a7); take 2 launched from that checkpoint with the remaining failures named and a turn-economy instruction. For phase 2a, which deletes rotation with the capsule: the deletion is now mandatory, not optional, and 2a itself must not run a Claude worker past 600 turns — it runs on deepseek-v4-pro (the codex driver emits one turn per invocation, so rotation never triggers) with an opus judge, or on Sonnet only with the same checkpoint safety net. For the spec (rule 4): a retried attempt in a fresh worktree must start from the sealed previous attempt branch, not from the run ref, or rotation must reuse the worktree. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T05:23:35.730Z
- [headless-worker-must-not-pause-20260908] Take 2 attempt 1 (Sonnet, 40 minutes, 140 turns, USD 4.84) ended with no worker result file: the worker started the ^[^ar] runner.test.mjs split, saw it exceed its ten-minute tool timeout, and ended its turn with 'I'll pause here and wait for the background test run to finish before proceeding further' — in a headless claude -p session ending the turn ends the session, so the controller found no canonical result, recorded protocol_failure as a gate rejection (revision 1 of 1) and re-dispatched attempt 2 in a fresh worktree from the run ref, again without the previous edits. The worker had committed its one fix on the attempt branch (599f7df: applyInvalidWorkerResult no longer persists its synthetic verdict to state.gate on a failover hop, so the next dispatch is a plain attempt and not a 'quality gate rejected' prompt; plus the rotation-markdown test now reads the attempt worktree). Run canceled at 06:12Z, campaign/lean-v03 fast-forwarded to 599f7df as the new checkpoint. Lessons folded into take 3: a worker must never end its turn to wait for anything — run every command in the foreground with an explicit tool timeout and split test runs so each finishes under eight minutes; and the exact failing list at the new checkpoint is measured by the orchestrator (full suite running now with INTENT_FACTORY_NOTIFY_BIN=/usr/bin/true so no desktop notification fires) instead of asking the worker to rediscover it. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T06:19:09.939Z
- [husky-prepare-breaks-snapshot-integrity-20260908] Root cause of take 3's snapshot_ignore_changed: the worker ran a package install in its attempt worktree (node_modules present, .husky/_ created at 07:25Z), the husky prepare hook wrote .husky/_/.gitignore, and captureIgnoreSources — which walks every .gitignore outside .git, .runs, node_modules, .claude and .codex — saw a new ignore source at the end of the attempt and failed the node before verification. Rules from now on: packets forbid npm install/npm ci in the worktree (tsc is global, the suite needs no dependencies), and phase 2a adds .husky/_ to the snapshot exclusions the same way .claude/worktrees was added after the same class of failure. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T08:00:34.521Z
- [deepseek-balance-exhausted-anthropic-only-20260908] Phase 2a take 1 terminal at 08:57Z: the deepseek-v4-pro worker failed with provider_error 'unexpected status 402 Payment Required: Insufficient Balance' (account balance USD -0.12) after about forty minutes, 22 files changed, 266 insertions, 4664 deletions (budget.mjs, capsule.mjs, handoff CLI and test, ledger, rotation and their tests removed; readers half adapted; typecheck 57 errors). Third provider-exhaustion class of the campaign after Z.ai 1310 and the Anthropic session limit: a prepaid balance only the owner can top up; owner notified by push. Decision: the partial diet is checkpoint a1370ac on campaign/lean-v03; phase 2a continues on claude-sonnet-5 with gate review none, because with one vendor left no cross-vendor judge exists and a same-vendor judge is forbidden; the full suite and the orchestrator's diff review remain the gate. Judges return to deepseek-v4-pro when the balance is restored. Codex models are available again since 04:56 BRT but stay out by owner instruction. Operational note: the working-tree runner at a1370ac already rejects the campaign journal's historical liveness facts (unexpected field weightedUsed), so campaign commands run from the controller-p2a snapshot until 2a makes the journal reader tolerant of them. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T09:13:06.479Z
- [worktree-needs-node-modules-20260908] Attempt worktrees have no node_modules, and test/ci-policy.test.mjs ('hooks are wired, executable, and enforce Conventional Commits') runs the commit-msg hook through commitlint from node_modules, so the repository-level test/*.mjs verification entry fails inside any attempt worktree even when the code is right (the 2a take-3 worker hit it and correctly refused to install). Decisions: the controller verification of every remaining contract drops test/*.mjs — the orchestrator runs it at phase close in the installed checkout — and phase 2b's continue-from-sealed-attempt node makes createAttemptWorktree symlink the repository's node_modules into new worktrees (excluded from the snapshot), which also removes the reason workers reached for npm install. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T11:42:47.492Z
- [seal-fails-on-ignored-sidecar-20260908] Blocking phase-1 defect found on the first non-empty seal of the campaign (phase 2b controller-lock, 2026-09-08T14:0xZ, after its five verification entries passed): sealAttempt in worktree.mjs ran , and git exits 1 with 'The following paths are ignored by one of your .gitignore files: .runs' (advice.addIgnoredFile) as soon as the attempt worktree holds the .runs/results sidecar every real worker writes; the replay fixtures never create that sidecar, so the suite was green while no real attempt could ever integrate — every earlier run had died before reaching the seal. Orchestrator hot-fix (a two-line change plus a named replay test, sealAttempt seals a worktree that holds the ignored .runs result sidecar): the pathspec is plain because is ignored in every execution repository (doctor requires it). The 2b run was canceled at 14:07Z with status-and-notify five minutes in; controller-lock's diff (18 files, 2027 insertions, 2497 deletions, verification 5 of 5 green) is adopted onto campaign/lean-v03 and the two remaining 2b nodes relaunch from a snapshot that contains the seal fix, so the integration transaction is finally exercised in the field. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T14:16:41.192Z
- [seal-fix-corrected-20260908] Correction to the seal hot-fix: dropping the exclude pathspec made sealAttempt commit the .runs result sidecar in fixture repositories that do not ignore .runs, which turned the empty attempt of 'resume judge recovery surfaces an invalid canonical result file' into a non-empty integrated one and let integration recovery adopt it on resume (the test had only ever passed because the old seal failed first). Final shape: git add -A of the worktree, then git rm --cached --ignore-unmatch of .runs, so the sidecar never enters an attempt commit whether or not the repository ignores it; the regression test asserts both a sealed change and the absent sidecar. Replay and integration suites 35 of 35 green with the corrected seal; full suite running before commit. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T14:33:25.875Z
- [changed-files-cap-20260908] Phase 2b take 3: status-and-notify finished in 85 minutes with all five verification commands green in its worktree (48, 48, 41, 75 runner tests and the skills test glob) and a done result, but the controller marked it blocked with protocol_failure because the result listed 33 changedFiles and the worker result protocol caps the list at 32 (worker-result.mjs). The cap is not a defect of the work and the protocol is a spec non-goal, so it stays; every following packet tells the worker to list at most 32 paths and to name directories when a diet touches more. The run was canceled at 16:15Z (continue-from-sealed-attempt had started from the pre-notify base and would have collided in runner.mjs); the orchestrator adopts the status-and-notify diff (21 files, 1108 insertions, 1061 deletions) after the full suite, and continue-from-sealed-attempt relaunches alone from the new base. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T16:20:24.062Z
- 16 earlier active decisions omitted

## User constraints

- Owner (2026-09-07 12:10 BRT): every successful phase is committed AND pushed, not only committed. The phase-close routine is therefore: full suite, review the diff, commit with Conventional Commits, push the campaign branch, record the outcome note, refresh the controller snapshot. Branch campaign/lean-v03 had never been pushed and was 59 commits ahead of origin/main; it was scanned for secret patterns and for .runs/.env leakage (clean, 81 files, all intent-factory plus AGENTS.md, README.md, package.json, tsconfig.json and test/ci-policy.test.mjs) and pushed with upstream tracking at cb3e26e. The repository is public, so the scan before the first push of any new range stays part of the routine. · f89ac1cf-fc09-4bba-a023-843a68440f44 · 2026-09-07T16:06:02.134Z
- Owner (2026-09-08 16:15 BRT): commit, hand off and document everything not yet recorded, because the campaign continues from another control harness. The Claude Code session fcc2f493 therefore closes phase 3, pushes, writes .runs/control/intent-factory-lean-20260905/ORCHESTRATOR-HANDOFF-20260908.md (runbook: state table, phase-4 launch steps, landing and adoption procedures, close routine, open issues) and PHASE-AUTHORING.md (rules learned on 2026-09-08), and stops. Standing owner directives: Anthropic and DeepSeek runtimes only; every successful phase committed and pushed; never idle after a terminal state; the test suite must never pop desktop notifications. · fcc2f493-0e59-43d7-a4e6-9a7b8958080a · 2026-09-08T19:26:22.518Z
- 5 earlier user constraints omitted

## Open questions

- Gap between a recorded campaign rule and what phase 0 actually shipped: the take-5 decision states that a retried attempt is always fresh and that the previous attempt's findings travel in the prompt, never through the provider session, because a reused dead session once returned success with zero turns and another produced no output for 900 s. The resume just issued re-dispatched parallel-and-fallback in mode 'reuse', so retry-in-place as shipped still continues the provider session. Blast radius is bounded — a zero-turn success would fail the node's deterministic verification rather than pass the gate — so attempt 3 is being observed rather than killed, and it will be resumed fresh if it stalls. Either the rule or the implementation must change in phase 2a; recorded here so the decision is made deliberately and not discovered again. · f89ac1cf-fc09-4bba-a023-843a68440f44 · 2026-09-07T18:27:19.186Z

## Linked runs

- intent-factory-lean-p3-visibility-20260908: 2 nodes · 1 failed · 1 done
  - dashboard-v2: failed · deterministic verification failed
- intent-factory-lean-p4-release-20260908: 3 nodes · 3 canceled
  - docs-v03: canceled · canceled
  - metrics-v03: canceled · canceled
  - runner-split: canceled · canceled
- intent-factory-lean-p4-release-20260908-take2: 3 nodes · 3 canceled
  - docs-v03: canceled · canceled
  - metrics-v03: canceled · canceled
  - runner-split: canceled · canceled
- 57 earlier attention-needed run states omitted
- intent-factory-lean-p2b-process-20260908-take5: 1 nodes · 1 done
- 2 earlier run summaries omitted

